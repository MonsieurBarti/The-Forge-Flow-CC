# Settings

Context: @references/orchestrator-pattern.md ∧ @references/conventions.md

## Steps
1. READ `.tff/settings.yaml` (if exists)
2. DISPLAY: model profiles + quality gate settings
3. ACCEPT changes: model profiles (quality/balanced/budget), quality gate (on/off, enforced/advisory), workflow toggles
4. WRITE updated `.tff/settings.yaml`
5. NEXT: @references/next-steps.md
